name: pipeline1

on:
  workflow_dispatch: 

jobs:
  my_job:
    
    name: MY JOB
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0 
      - name: hi
        env:
          stmnt: My India
          
        run: echo "Hello World, ${{ vars.first_name}} ${{ secrets.last_name}} $stmnt"

      - name: list files
        run: ls -a
